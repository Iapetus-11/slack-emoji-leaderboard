name: CI

on:
  push:
  pull_request:
    types: [opened]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: setup python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: setup node 18
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: install autoflake, black, isort, and flake8
        run: pip install autoflake black isort flake8

      - name: load cached frontend node_modules
        id: cached-npm-dependencies
        uses: actions/cache@v2
        with:
          path: ./frontend/node_modules
          key: venv-${{ runner.os }}-${{ hashFiles('**/yarn.lock') }}

      - name: install frontend dependencies
        working-directory: ./frontend
        run: yarn

      - name: auto-flake
        run: autoflake --remove-all-unused-imports --expand-star-imports --ignore-init-module-imports --in-place --recursive backend/src/ sync/src/

      - name: black fix
        run: black backend/src/ sync/src/

      - name: isort fix
        run: isort backend/src/ sync/src/

      - name: prettier fix
        working-directory: ./frontend
        run: yarn format

      - name: auto commit
        if: github.ref != 'refs/heads/main'
        uses: stefanzweifel/git-auto-commit-action@v4.15.4
        with:
          commit_message: format / fix code

      - name: flake8
        run: flake8 backend/src/ sync/src/
